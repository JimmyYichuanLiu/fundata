# FundTrack 升级验收记录

验收日期：2026-09-06。全部开发、数据库备份、导出和本地测试产物位于 `D:\coding\fundata`。公网服务器尚未执行本轮发布，本记录不能作为已上线证明。

## 本地数据与真实邮箱

迁移副本验证：原始 64,988 条记录前后数量不变；64,931 条有效、57 条隔离、340 只有效基金；净值截止 2026-07-31。迁移和重复执行完整性检查通过，审计证据保留。原库随后使用相同入口备份并迁移。

实际 IMAP 增量运行（北京时间 22:25–22:27）：

| 项目 | 结果 |
|---|---:|
| 处理邮件 | 684 |
| 新增净值 | 572 |
| 重复净值 | 3,112 |
| 提取失败项 | 219 |
| 本轮状态 | 部分成功 |
| 最新有效净值日期 | 2026-09-04 |
| 更新后有效净值总数 | 65,503 |
| 其中有效邮件净值 | 8,858 |

失败项分类：128 项格式未识别、36 项产品代码或日期字段不完整、25 项日期无效、25 项净值无效、5 项 Excel 文件格式无法确定。失败记录可在本地管理员界面查看原邮件、附件和工作表定位，不能宣称全部邮件数据已恢复。此前 57 条隔离记录不因新同步而删除。

紧接着第二次真实增量检查完整成功：处理 0、新增 0、重复 0、失败 0，验证已提交检查点不会重复导入。它只证明此次没有新邮件，不代表上一轮失败已解决。

最初受本地网络隔离影响的一次连接失败正确记为 `error`；授权网络访问后上述真实同步完成，没有把连接失败显示为成功。

## Excel

最终本地文件：`outputs/fundtrack-upgrade/fund_email_nav.xlsx`。由项目保留的导出功能生成，包含 77 个工作表（汇总 + 76 只基金），8,858 条有效邮件净值。所有分表行数和汇总记录数一致，Excel 错误单元格扫描为 0；已检查汇总渲染、表头、基金名称和数值格式。实际文件不提交 GitHub。

## 自动验证

- 后端/数据/权限/导出/部署单元与集成测试：263 项通过。
- 前端计算与状态单元测试：125 项通过。
- 前端生产构建：通过，React、图表与应用拆包。
- Chrome 模拟接口端到端：6 项通过，包括八基金 URL 恢复、管理员写入 CSRF、历史字段及窄屏。
- 真实后端端到端：6 条流程连续三轮，18 项通过。使用数据库副本与一次性管理员，不触发真实同步或失败重试；测试组合在副本中创建并清理。
- 三尺寸 1440/768/375、浅色/深色，覆盖首页、详情、对比、组合构建、管理员、市场、基差，42 张视觉检查图保存在忽略的测试目录。修复对比首列和手机基差日期挤断；未发现页面运行错误或整页横向溢出。

测试说明：旧迁移测试不再要求擦除已有策略/元数据、不再把 ZX 的空来源关联误认为手工数据，改为验证保留；可选真实快照测试使用 SQLite backup API，避免遗漏 WAL。既有原油/新闻开启状态的历史“修改前”测试及未配置的老浏览器脚本不是本轮验收套件。

审查修正包括：会话认证阻塞、DDL 事务回滚、邮件单封事务与检查点、同日冲突、未来日期、过期同步恢复、净值修改后的复权重算、缺失持仓估值、再平衡当日收益，以及组合缓存过期识别。投资指标参数保持原模块口径，见 `DATA_CONVENTIONS.md`。

## 发布边界与待办

- GitHub 分支为 `codex/fundtrack-upgrade`，仅提交相关源码、测试、配置模板和文档；不包含数据库、邮件附件、备份、凭据、真实 Excel 或浏览器产物。
- HTTP 公网采用匿名只读，管理员功能本地验证通过；服务端权限与 nginx 模板双层限制写操作/邮件详情。
- 发布工具要求准确项目/数据库/服务/40 位提交，保持单 worker，迁移前备份，启动后检查匿名写操作拒绝。拒绝自动回滚至没有权限保护的旧版。
- 原模板曾包含第三方 API 密钥，已从模板移除；历史 Git 记录仍可能包含旧值，维护者需要撤销/轮换该密钥，不应把移除文件内容等同于凭据已失效。
- **尚待用户在腾讯云控制台完成**：核实路径与服务、安装审核后的只读 nginx 配置、按固定提交发布、使用服务器自身邮箱配置同步、核对公网最新净值及匿名写操作拒绝。具体步骤见 `DEPLOYMENT.md`。
